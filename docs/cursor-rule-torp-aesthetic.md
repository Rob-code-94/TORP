# TORP Cursor Rule

The canonical rule file is checked into the repo at:

- `.cursor/rules/torp-aesthetic.mdc`

If you ever need a backup copy/paste source, the contents should match that file exactly.

### Optional: make it always-on

If you want the rule to apply even when you’re not editing matched files, set:

- `alwaysApply: true`
